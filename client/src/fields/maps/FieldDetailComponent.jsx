import React from 'react'
import PropTypes from 'prop-types';
import {withGoogleMap, GoogleMap, Marker} from 'react-google-maps'

const FieldDetailComponent = ({ fieldID }) => (
    <div>
        
    </div>
);

export default FieldDetailComponent;